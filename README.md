# Фото-Открытки - Telegram Mini App

React приложение для создания персонализированных открыток с помощью AI.

## Установка

```bash
npm install
```

## Настройка

Создайте файл `.env` в корне проекта:

**Для Replicate (по умолчанию):**
```
VITE_REPLICATE_API_KEY=your_replicate_api_key_here
REPLICATE_API_KEY=your_replicate_api_key_here
```

**Для выбора модели (опционально):**
```
VITE_REPLICATE_MODEL=google/nano-banana  # или black-forest-labs/flux-pro
```

По умолчанию используется `google/nano-banana`. Для использования Flux Pro установите:
```
VITE_REPLICATE_MODEL=black-forest-labs/flux-pro
```

Получите API ключ на [Replicate.com](https://replicate.com/)

**Примечание:** 
- По умолчанию используется модель Nano Banana (`google/nano-banana`) на Replicate
- Можно переключиться на Flux Pro, установив `VITE_REPLICATE_MODEL=black-forest-labs/flux-pro` в `.env`

## Запуск

**Важно:** Нужно запустить два сервера - frontend и backend.

1. **Установите зависимости:**
```bash
npm install
```

2. **Запустите backend сервер (в одном терминале):**
```bash
npm run dev:server
```

3. **Запустите frontend (в другом терминале):**
```bash
npm run dev
```

Backend сервер будет работать на `http://localhost:3001`, frontend на `http://localhost:3000`.

## Сборка

```bash
npm run build
```

## Деплой для Telegram Mini App

1. Соберите приложение: `npm run build`
2. Загрузите папку `dist` на хостинг с HTTPS (например, Vercel, Netlify, или свой сервер)
3. В BotFather настройте Mini App:
   - Используйте команду `/newapp` или `/editapp`
   - Укажите URL вашего приложения (например, `https://your-app.vercel.app`)
   - Настройте иконку и описание

## Стили открыток

Приложение поддерживает 5 предустановленных стилей:
- 🎄 Новогодняя
- ✈️ Путешествие
- 💕 Романтическая
- 🎂 День рождения
- 🌲 Природа

Каждый стиль имеет свой промпт для AI генерации, оптимизированный для создания качественных открыток.

## Технические детали

- **Frontend**: React 18 + Vite
- **Backend**: Express.js (прокси для обхода CORS)
- **AI генерация**: Replicate API (Nano Banana по умолчанию, можно переключить на Flux Pro)
- **Telegram интеграция**: Telegram WebApp SDK
- **Стили**: CSS с градиентами и backdrop-filter для современного вида

## Решение проблемы CORS

Приложение использует backend прокси сервер для обхода CORS ограничений Replicate API. API ключ хранится на сервере, что безопаснее, чем на клиенте.

## Выбор модели генерации

Приложение использует Replicate API и поддерживает две модели:

1. **Nano Banana (по умолчанию)** - `google/nano-banana` - передовая модель от Google DeepMind
2. **Flux Pro** - `black-forest-labs/flux-pro` - высококачественная модель для фотореалистичной генерации

Для переключения между моделями установите в `.env`:
- `VITE_REPLICATE_MODEL=google/nano-banana` (по умолчанию)
- `VITE_REPLICATE_MODEL=black-forest-labs/flux-pro` (для Flux Pro)

**Важно:** Обе модели требуют API ключ Replicate и достаточный баланс на аккаунте.

